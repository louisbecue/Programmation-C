#ifndef MAXLINE
#define MAXLINE 100
#endif

extern int readl(char line[]);

void rev(char tab[])
{
    int i, c;
    readl(tab);
    i = 0;
    while(i < MAXLINE){
        
    }       
}